import ProductDetail from "@/components/index/sections/productDetail";

export default function ProductDetailTemplate({detailData, relatedData}) {
    return <ProductDetail detailData={detailData} relatedData={relatedData} />;
}
